# Phase 43 完成

## 需求分析
- 首次进入侧栏空白，易误判为「没有好友」
- 打开会话加载历史时无反馈

## 设计计划
- `sidebarLoading`：骨架屏行（头像+两行文字）
- `historyLoading`：打开会话 / 加载更早时按钮文案与消息区骨架
- 空状态文案区分「加载中」与「真的没有好友」

## 交付
- `frontend/src/views/ChatView.vue`：骨架 UI 与加载状态

## 测试
```powershell
cd frontend; npm run build
```

## 验证
- 刷新聊天页侧栏先出现骨架再出列表
- 点击会话时若历史未到，消息区显示骨架
- 「加载更早消息」点击后显示「加载中…」

## 下阶段
Phase 44：消息时间精确到秒的可选显示 / 双击气泡复制
