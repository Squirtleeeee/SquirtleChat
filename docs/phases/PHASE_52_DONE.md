# Phase 52 完成

## 需求分析
- 无法批量选中消息做转发准备
- 先做「多选 + 复制为可粘贴文本」，不接跨会话服务端转发

## 设计计划
- 会话头「多选」进入选择模式
- 勾选消息；底栏显示已选数量
- 「复制转发文本」按时间序拼 `发送者 时间: 内容` 到剪贴板

## 交付
- `frontend/src/views/ChatView.vue`：多选模式、勾选、复制转发文本

## 测试
```powershell
cd frontend; npm run build
```

## 验证
- 点「多选」出现勾选框
- 选中若干条后「复制转发文本」成功，提示已复制
- 切换会话退出多选

## 下阶段
Phase 53：会话内「清空本地消息缓存」（不删服务端）
